(function() {
  'use strict';
  
  // Vendor файл - заглушка для совместимости с HTML шаблонами
  // Все vendor библиотеки теперь встроены в trudesk.min.js
  console.log('Vendor libraries loaded via trudesk.min.js');
  
  // Убеждаемся, что глобальные переменные доступны
  if (typeof window !== 'undefined') {
    // Vendor libraries будут загружены через trudesk.min.js
  }
})();

